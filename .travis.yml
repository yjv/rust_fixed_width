sudo: false
language: rust
rust:
  - stable
  - beta
  - nightly
before_script: |
  PKGNAME="rust_fixed_width" # must match with cargo.toml
  LOCAL="~/.local" # install here to avoid `sudo`
  KCOV_VERSION=33
  export PATH=$LOCAL/bin:$PATH
script: cargo test --verbose
addons:
  apt:
    packages:
    - libcurl4-openssl-dev
    - libelf-dev
    - libdw-dev
    - libbfd-dev # required for `--verify`
after_success: |
  wget https://github.com/SimonKagstrom/kcov/archive/v$KCOV_VERSION.tar.gz &&
  tar xzf v$KCOV_VERSION.tar.gz &&
  mkdir kcov-$KCOV_VERSION/build &&
  cd kcov-$KCOV_VERSION/build &&
  cmake -DCMAKE_INSTALL_PREFIX:PATH=$LOCAL .. &&
  make &&
  make install &&
  cd ../.. &&
  for f in target/debug/rust_fixed_width-*;do test -x $f && mv $f target/debug/rust_fixed_width; done &&
  kcov --verify \
       --coveralls-id=$TRAVIS_JOB_ID \
       --exclude-pattern=/.cargo,/usr/lib \
       target/kcov target/debug/rust_fixed_width
  bash <(curl -s https://codecov.io/bash) &&
  echo "Uploaded code coverage"
